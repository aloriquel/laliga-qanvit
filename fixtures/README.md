# Fixtures

Place test PDF decks here for smoke testing.

## sample-deck.pdf

A demo startup pitch deck used by `npm run smoke:pipeline`.

**How to get one:** Use any publicly available startup deck PDF with selectable text (not scanned).
The file is gitignored to avoid committing potentially confidential material.

Requirements:
- Must be a real PDF with selectable text (not a scan)
- Minimum ~3 pages, ideally 10-20 pages
- Any startup category works — the classifier will detect the vertical
